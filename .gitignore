# Node modules(Dependencias) --> No sube las dependencias (se instalan con npm install)
node_modules
# Env(Variables de entorno) --> No sube variables de entorno (contraseñas, API keys)
.env
# Os junk(Archivos del sistema) --> Archivo oculto de macOS que no sirve para el proyecto
.DS_Store

Codex.dmg
